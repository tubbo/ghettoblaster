= form_for @subscriber do |f|
  = f.text_field :name, placeholder: 'Name'
  = f.email_field :email, placeholder: 'Email Address'
  .right
    = f.submit 'Add me to the guest list!', class: 'success button round'
